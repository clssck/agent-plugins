## Authentication Policies Configuration

### Inherited Values

These values are inherited from Platform Foundation. Any changes apply only to this account.

| Setting | Value | Source |
|---------|-------|--------|
| **Human Authentication** | {{ human_auth_methods | join(', ') }} | Platform Foundation (editable) |
| **MFA Method** | {{ mfa_method | join(', ') }} | Platform Foundation (editable) |
| **Service Authentication** | {{ service_auth_methods | join(', ') }} | Platform Foundation (editable) |
| **Account-Level Enforcement** | {{ apply_auth_policies_account_level }} | Platform Foundation (editable) |

### Policy Summary

| User Type | Policy Name | Authentication Methods |
|-----------|-------------|------------------------|
| **Human Users** | `human_user_auth_policy` | {{ human_auth_methods | join(', ') }} |
| **Service Accounts** | `service_account_auth_policy` | {{ service_auth_methods | join(', ') }} |
| **Break-Glass** | `breakglass_auth_policy` | Password Only |

### Human User Authentication Policy

{%- if "SAML (SSO)" in human_auth_methods and "Password with MFA" not in human_auth_methods %}
- Authentication: SAML only
- Users must use SSO via {{ identity_provider }}
- MFA handled by Identity Provider
{%- elif "SAML (SSO)" in human_auth_methods and "Password with MFA" in human_auth_methods %}
- Authentication: SAML or Password
- MFA: {{ mfa_method | join(', ') }} (for password logins)
- Users can choose SSO or password + MFA
{%- else %}
- Authentication: Password only
- MFA: {{ mfa_method | join(', ') }} (required)
- Users authenticate directly with Snowflake
{%- endif %}

### Service Account Authentication Policy

{%- if service_auth_methods | length == 1 and "OAuth" in service_auth_methods %}
- Authentication: OAuth only
- Requires OAuth integration
{%- elif service_auth_methods | length == 1 and "Key Pair" in service_auth_methods %}
- Authentication: Key-pair only
- No passwords to manage
- Most secure for automated access
{%- elif "OAuth" in service_auth_methods and "Key Pair" in service_auth_methods and "PAT" not in service_auth_methods %}
- Authentication: OAuth or key-pair
- Flexibility for different integration types
{%- else %}
- Authentication: {{ service_auth_methods | join(', ') }}
- Maximum flexibility including personal access tokens
{%- endif %}

### Policy Application

{%- if apply_auth_policies_account_level == "Yes - Apply default policy to all users" %}
**Account-Level Enforcement: Enabled**
- The human user authentication policy applies to all users by default
- Break-glass accounts must have their own policy explicitly assigned
- Service accounts should have the service policy explicitly assigned
{%- else %}
**Account-Level Enforcement: Disabled**
- Policies are created but not applied at account level
- Assign policies to users as needed
- Consider phased rollout starting with admin users
{%- endif %}

### Important Notes

- Break-glass accounts have their own policy allowing password-only access
- Admin users should use the human authentication policy
- Apply service policy only to designated service accounts
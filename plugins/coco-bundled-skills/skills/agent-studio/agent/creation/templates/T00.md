# T00 — Empty / Placeholder Agent

No tools, no data access. The agent can only respond using its base model.
Use this when the user explicitly wants a minimal or placeholder agent.

## Spec

```yaml
{}
```
